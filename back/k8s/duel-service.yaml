apiVersion: apps/v1
kind: Deployment
metadata:
  name: duel-service
  namespace: pokemon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: duel-service
  template:
    metadata:
      labels:
        app: duel-service
    spec:
      containers:
      - name: duel-service
        image: duel-service:latest
        imagePullPolicy: Never
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: duel-service
  namespace: pokemon
spec:
  selector:
    app: duel-service
  ports:
  - port: 8004
    targetPort: 8000